# Submission 032-037 Report (2026-05-09)

## Kaggle Status Snapshot

本轮按“5 次提交”执行，共提交 5 个文件（含队列中历史补齐的首条）并等待均已 `SubmissionStatus.COMPLETE`：

| No | 文件名 | 方式 | Public score |
| --- | --- | --- | ---: |
| 1 | `submission_032_qwen_evi_mp3_r05_s005.csv` | Qwen VLM answer + baseline evidence | 0.27 |
| 2 | `submission_033_rules_from_vision.csv` | 基于 `033` 的规则重解码（vision OCR cache） | 0.24 |
3 | `submission_034_rules_from_easyocr.csv` | 基于 `033` 的规则重解码（easyocr cache） | 0.24 |
| 4 | `submission_035_rules_from_tesseract.csv` | 基于 `033` 的规则重解码（tesseract cache） | 0.24 |
| 5 | `submission_037_anchor_from_034_union_min4.csv` | 锚定 evidence 合并（max_pages=2,min_score=4, include_fallback_pages） | 0.22 |

最高分维持 `0.27`，未出现高于现有 0.28 的方案。

## 实验流程

- 先查询当日提交列表，仅剩可用额度后生成 5 条候选。
- 生成候选时保留同一答案主线（主要来自 `submission_032`）：
  - `make_answer_rules_from_base.py` 生成 033/034/035；
  - `make_answer_anchored_submission.py` 生成 037（基于 034 的 evidence 回锚定）；
  - 维持 open/compliant 流程，无新增私有模型推理步骤。
- 提交后持续轮询 `kaggle competitions submissions`，按实际返回状态记录分数。

## 结果解读

- `submission_032` 的 0.27 已低于历史 0.28，说明当期基线又有回落风险，建议优先固定更强复现基线作为 anchor。
- 证据重解码/换回 OCR cache 的方向在 `033~035` 均回落到 0.24，说明当前阶段仅换 evidence cache 的收益不足；
- `submission_037` 的锚定方式未提升，分数继续下滑到 0.22，后续应减少这类高强度 evidence 约束策略优先级。

## 下一步

1. 回退至历史最高的稳定线（含 0.28 的公开高分）做对照并固定版本；
2. 继续只在规则层面进行轻量级可逆修改，避免大范围证据重排；
3. 同时关注代码/讨论更新中是否出现与物理页码映射或问题类型分桶相关的新启发。
